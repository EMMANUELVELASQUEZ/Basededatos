CREATE TABLE FACTURAS
(
FOLIO Smallint Not Null,
FECHA DATE,
SUBTOTAL DECIMAL(10,2),
IVA DECIMAL(8,2),
CVECLI SMALLINT
);

CREATE TABLE CLIENTES
(
CVECLI Smallint Not Null,
NOMCLI CHAR(45),
DIR VARCHAR(45),
DIR VARCHAR(60)
);
 


CREATE TABLE LINEAS
(
CVELINEA Smallint Not Null,
NOMLINEA CHAR(25)
);

CREATE TABLE ARTICULOS
(
CVEART Smallint Not Null,
NOMBRE CHAR(25),
PRECIO DECIMAL(8,2),
EXISTENCIAS SMALLINT,
CVELINEA SMALLINT
);


CREATE TABLE PARTIDAS
(
PARTIDA Smallint Not Null,
FOLIO SMALLINT,
CVEART SMALLINT,
PVENTA DECIMAL(8,2),
CANTIDAD SMALLINT
);


/*
AGREGA LA COLUMNA DEPENDIENTES CON TIPO SMALLINT PARA LA TABLA PERSONAL
*/
ALTER TABLE PERSONAL ADD DEPENDIENTES SMALLINT;


/*
AÑADE UNA LLAVE PRIMARIA A LA TABLA DEPTOS EN LA COLUMNA CLAVEDEP
/*

ALTER TABLE DEPTOS ADD PRIMARY KEY(CLAVEDEPTO);

--------------------------------------------------------
llaves primarias y foraneas
------------------------------------------------------


ALTER TABLE `articulos` ADD PRIMARY KEY(`CVEART`);

ALTER TABLE `clientes` ADD PRIMARY KEY(`CVECLI`);

ALTER TABLE `facturas` ADD PRIMARY KEY(`FOLIO`);

ALTER TABLE `partidas` ADD PRIMARY KEY(`FOLIO`);


SELECT * FROM `articulos` (EN SI ESTE CODIGO SE UTILZA EN SI PARA ABRIR TABLA Y REVISAR)























Insert into LINEAS values (1, “ROPA”)

Insert into LINEAS values (2, “ELECTRODOMÉSTICOS”)


Insert into ARTICULOS values (111, “CAMISOLA MEZCLILLA”, 350, 12, 3)

Insert into ARTICULOS values (112, “CAFETERA EXPRESSO”, 1200, 10, 2)



 insert into FACTURAS values (1001,"12/12/!2",1000,160,1)



