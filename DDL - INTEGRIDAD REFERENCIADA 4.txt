DDL - INTEGRIDAD REFERENCIADA 4

9/09/2024

créate table partidas(

NUM_PARTIDAS INT,
FECHAINI DATE,
DURACION INT,
ID_TORNEO INT
);

CREATE TABLE TORNEOS (
ID_TORNEO INT,
NOM VARCHAR (30)
PERIODICIDAD TEXT,
FECHA_CREA DATE,
FECHAINI DATE,
CUANTIA INT,
GANADOR VARCHAR(30),
PAIS INT
);

CREATE TABLE MOVIMIENTOS (
ORDINAL    (PK),
CASILLA_ORIGEN INT,
CASILLA_DESTINO  INT,
NUM_PARTIDA INT
);

CREATE TABLE JUEGAN (

NUM_PARTIDA INT,
NUM_FEDERADO INT,
COLOR INT
);

CREATE TABLE FEDERACIONES(
PAIS (PK)
ID_FEDERACION VARCHAR(30),
NOM VARCHAR (30),
TEL VARCHAR(10),
FECHA_FUND DATE,
PAIS INT,
ID_FEDERACION
);


CREATE TABLE JUGADOREES(

NUM_FEDERADO ,
NOM VARCHAR(30),
APELLIDOS VARCHAR(20),
DIR_POSTAL VARCHAR (50),
DIR_ELECTROMICA VARCHAR(50),
PAIS INT,
ID_FEDERACION INT
);
